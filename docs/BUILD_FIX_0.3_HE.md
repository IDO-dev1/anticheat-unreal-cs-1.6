# תיקוני בנייה v0.3

השגיאה `invalid use of incomplete type usercmd_s` נפתרת באמצעות `#include <usercmd.h>` אחרי `extdll.h`.

בנוסף תוקנה בעיית ארכיטקטורה: כל היעדים המקושרים ל-SO נבנים עם `-m32`.

מומלץ להחליף את כל הפרויקט ולא לערבב קבצים מ-v0.2.x.
